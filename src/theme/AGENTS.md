# AGENTS.md - Theme

This folder contains styles, theme utilities, color logic, and CSS rendering helpers.

- Keep styling tokens and render helpers consistent with the repo's existing theme model.
- Avoid hard-coded colors when an existing theme variable or Home Assistant token fits.
- Preserve visual behavior across supported Home Assistant themes.
- Add tests for non-trivial color, threshold, or style-conversion logic.
